// loops - ex5.js

let array = [1, 2, 3, 4];
let index = 0;

while (index < array.length) {
  console.log(array[index]);
  index += 1;
}

// If the array was empty, array.length would be 0 and the while loop would never execute, so the code would not output anything.