// loops - ex10.js

/* 
// Snippet 1
let counter = 0;

while (counter > 0) {
  console.log('Woooot!');
  counter -= 1;
}

// Snippet 2
let counter = 0;

do {
  console.log('Woooot!');
  counter -= 1;
} while (counter > 0);
*/

// In snippet 1, nothing is logged because the condition for the while loop is not true. In snippet 2 Wooot! is logged once to the console because a do...while loop unlike a while loop always runs once before the condition for while is checked which in Snippet 2's case false.